<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sole.star">

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HZSeekBarActivity" />
        <activity android:name=".ui.InterpolatorTestActivity" />
        <activity android:name=".ui.PwdActivity" />
        <activity android:name=".ui.ExpandableTextActivity" />
        <activity android:name=".ui.greendao.GreenDaoTestActivity" />
        <activity android:name=".ui.LeanTextActivity" />
        <activity android:name=".ui.ScrollToTestActivity" />
        <activity android:name=".ui.ScrollLayoutActivity"></activity>
    </application>

</manifest>